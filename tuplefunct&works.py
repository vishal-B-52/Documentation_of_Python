tupler = ("Lister", "free", "fire", "water", "fake one")

""" Tuples are limited to their classes. Once they are set with their values, one cannot add, remove ,insert, revert 
values inside it. Even if you wanted do anything from above, this classes won't allow you to it."""

print(tupler.count('free'))  # counts how many times a word has appeared.
print(tupler.index("water"))  # tells index of specified element
