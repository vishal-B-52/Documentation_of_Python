lister = "I am Vishal. I am maker of this string."
lister.capitalize()  # Capitalizes the first word of any element.
lister.upper()  # Turns every letter to capital.
lister.lower()  # Turns every character to lower case.
# lister.count()  # Counts how many times a word has appeared in the string.
# lister.startswith()  # return true or false value if the string is starting with specifies letter or word.
# lister.index()  # displays the character on the specified position.
# lister.endswith()  # return true or false value if the string is ending with specifies letter or word.
lister.swapcase()  # reverses upper case to lower and lower to upper.
lister.title()  # turns every character after full-stop to uppercase.
# lister.find()  # finds the specified object and returns whole string after it.
# lister.replace()  # replaces wanted character with existing character. requires two arguments for replacement.
lister.split()  # splits one string into two and encases it into a list. requires only one argument.
lister.isupper()  # returns boolean values if all characters are in uppercase.
lister.islower()  # returns boolean values if all characters are in lowercase.
lister.isalpha()  # returns boolean value if string is alphanumeric. Alphanumeric means a string without space.
lister.isspace()  # returns true if characters contain spaces.
lister.isdigit()  # returns true if string contains digits


